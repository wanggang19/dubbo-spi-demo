package com.dubbo.spi.demo.consumer;

/*import com.dubbo.spi.demo.consumer.service.Robot;
import org.apache.dubbo.common.URL;
import org.apache.dubbo.common.extension.ExtensionLoader;
import org.testng.annotations.Test;*/

import java.util.HashMap;
import java.util.Map;

/**
 * @Auther: wanggang(323492)
 * @Date: 2021/2/6 11:54
 * @Description:
 */
/*public class DubboSpiTest {
    public static void main(String[] args) {
        ExtensionLoader<Robot> extensionLoader =
                ExtensionLoader.getExtensionLoader(Robot.class);
        Robot robot = extensionLoader.getExtension("robotWrapper");
        Map<String, String> paramMap = new HashMap<>();
        paramMap.put("robot", "bumblebee");
        URL url = new URL(null, null, 0, paramMap);
        robot.sayHello(url);
    }
}*/
